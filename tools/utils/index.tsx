"use client"

import Moment from "./moment"
import Format from "./format"
import Mask from "./mask"
import RemoveMask from "./remove-mask"

export class Utils {
    static moment = Moment
    static format = Format
    static mask = Mask
    static removeMask = RemoveMask
}